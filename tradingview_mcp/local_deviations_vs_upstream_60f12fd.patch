diff --git a/.gitignore b/.gitignore
index 57fda66..ef832d1 100644
--- a/.gitignore
+++ b/.gitignore
@@ -20,3 +20,9 @@ dist/
 # scratch screenshots written by mount_pine.py / tv_screenshot
 moon_dev_chart.png
 _debug_state.png
+
+# local virtualenv (created 2026-09-14; never commit)
+venv/
+
+# Claude Code per-machine approvals (created 2026-09-14); .mcp.json itself IS meant to be committed
+.claude/settings.local.json
diff --git a/mount_pine.py b/mount_pine.py
index fb197ed..04ff3fb 100644
--- a/mount_pine.py
+++ b/mount_pine.py
@@ -66,7 +66,7 @@ async def main() -> int:
     shot = Path(args[args.index("--shot") + 1]) if "--shot" in args else Path("moon_dev_chart.png")
     keep_editor = "--keep-editor" in args
 
-    code = pine.read_text()
+    code = pine.read_text(encoding="utf-8")
     mcp = build_server()
 
     print(f"🌙 Moon Dev — mounting {pine.name} ({len(code)} bytes)")
diff --git a/requirements.txt b/requirements.txt
index 684857e..59960b2 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,7 +1,7 @@
 # 🌙 Moon Dev's TradingView MCP Server — dependencies
 # Standalone MCP. Nothing here depends on any other MCP server.
 
-mcp>=1.0.0
+mcp>=1.0.0,<2  # 2.x renamed FastMCP -> MCPServer; server.py imports mcp.server.fastmcp (pinned locally 2026-09-14)
 websocket-client>=1.7.0
 requests>=2.31.0
 pydantic>=2.0
diff --git a/tradingview_mcp/tools/chart.py b/tradingview_mcp/tools/chart.py
index aef2ddb..b5d110f 100644
--- a/tradingview_mcp/tools/chart.py
+++ b/tradingview_mcp/tools/chart.py
@@ -18,12 +18,32 @@ from ._helpers import focus_chart, press_escape, with_cdp
 
 
 def _current_chart_state(cdp: CDPClient) -> dict[str, str]:
-    """Scrape symbol + interval from the page title and URL. TV encodes both."""
+    """Scrape symbol + interval from the page title and URL. TV encodes both.
+
+    2026-09-14: on tradingview.com/chart/ the title is just the ticker ("AAPL")
+    and the URL carries no query, so both original sources come back empty.
+    Added DOM fallbacks: the header symbol button (ticker) and the main-series
+    legend row (interval / exchange / description). The header interval bar is
+    NOT used first: its aria-checked button disagreed with the legend on a live
+    chart (toolbar said 15m, legend and candles said 1D).
+    """
     js = """
     (() => {
-      const title = document.title || '';
-      const href = location.href || '';
-      return { title: title, href: href };
+      const txt = (el) => el ? (el.innerText || el.textContent || '').replace(/\\s+/g, ' ').trim() : '';
+      const q = (s) => document.querySelector(s);
+      const series = q('[class*="legend-"] [class*="item-"][class*="series-"]');
+      const sub = (s) => series ? series.querySelector(s) : null;
+      const hb = q('#header-toolbar-intervals [aria-checked="true"]');
+      return {
+        title: document.title || '',
+        href: location.href || '',
+        symbol_button: txt(q('#header-toolbar-symbol-search')),
+        interval_legend: txt(sub('[class*="intervalTitle"]')),
+        interval_header: hb ? (hb.getAttribute('data-value') || txt(hb)) : '',
+        exchange: txt(sub('[class*="exchangeTitle"]')),
+        description: txt(sub('[class*="mainTitle"] [class*="title-"]')) || txt(sub('[class*="mainTitle"]')),
+        series_title: txt(sub('[class*="titlesWrapper"]')),
+      };
     })()
     """
     data = cdp.eval_js(js) or {}
@@ -38,12 +58,36 @@ def _current_chart_state(cdp: CDPClient) -> dict[str, str]:
             symbol = parts[0]
             if len(parts) >= 2:
                 interval = parts[1]
-    # Fallback: parse ?symbol=&interval= from URL.
+    # Fallback 1: header symbol button + main-series legend row (current TV DOM).
+    # The legend row has two display modes: full ("Apple Inc 1D NASDAQ", separate
+    # intervalTitle/exchangeTitle spans) and compact ("BATS:AAPL · 15", one title).
+    # Parse the compact form before trusting the header bar, which has disagreed
+    # with the chart on a live session.
+    import re
+    series_title = str(data.get("series_title", ""))
+    exchange = str(data.get("exchange", ""))
+    m_iv = re.search(r"(?<![\w:])(\d+[smhHDWM]?)(?![\w:!])", series_title)
+    m_ex = re.search(r"(?<![\w:])([A-Z0-9_]+):([A-Z0-9_.!&-]+)", series_title)
+    symbol = symbol or str(data.get("symbol_button", "")) or (m_ex.group(2) if m_ex else "")
+    interval = (
+        interval
+        or str(data.get("interval_legend", ""))
+        or (m_iv.group(1) if m_iv else "")
+        or str(data.get("interval_header", ""))
+    )
+    exchange = exchange or (m_ex.group(1) if m_ex else "")
+    # Fallback 2: parse ?symbol=&interval= from URL.
     from urllib.parse import urlparse, parse_qs
     qs = parse_qs(urlparse(href).query)
     symbol = symbol or (qs.get("symbol", [""])[0])
     interval = interval or (qs.get("interval", [""])[0])
-    return {"symbol": symbol, "interval": interval, "url": href}
+    return {
+        "symbol": symbol,
+        "interval": interval,
+        "url": href,
+        "exchange": exchange,
+        "description": str(data.get("description", "")),
+    }
 
 
 def register(mcp) -> None:
diff --git a/tradingview_mcp/tools/indicators.py b/tradingview_mcp/tools/indicators.py
index 3cf226a..f44d76b 100644
--- a/tradingview_mcp/tools/indicators.py
+++ b/tradingview_mcp/tools/indicators.py
@@ -41,17 +41,31 @@ def _close_dialog(cdp: CDPClient) -> None:
 
 
 def _read_legend(cdp: CDPClient) -> list[dict]:
-    """Scrape the chart legend for currently attached studies and their values."""
+    """Scrape the chart legend for currently attached studies and their values.
+
+    2026-09-14: `[data-name="legend-source-item"]` matches nothing on current
+    TradingView (tv_selectors.py marks it dead since 2026-08-29), so this
+    returned [] on a chart that visibly had studies. Now reads the hashed
+    CSS-module legend first (S.LEGEND_STUDY_TITLE_V2 family, the same selector
+    mount_pine.py uses) and keeps the old selector as a fallback. Each row gets
+    `title` (first legend line), `args` (the remaining lines, e.g. inputs) and
+    `values` (the plotted values as displayed).
+    """
     js = """
     (() => {
-      const items = Array.from(document.querySelectorAll('[data-name="legend-source-item"]'));
-      return items.map((el, idx) => {
-        const titleEl = el.querySelector('[data-name="legend-source-title"]');
-        const valueEls = Array.from(el.querySelectorAll('[class*="valueItem"]'));
+      const norm = (s) => (s || '').replace(/[\\u00a0\\u202f]/g, ' ').trim();
+      let rows = Array.from(document.querySelectorAll('[class*="sources-"] > [class*="study-"]'));
+      if (!rows.length) rows = Array.from(document.querySelectorAll('[data-name="legend-source-item"]'));
+      return rows.map((el, idx) => {
+        const t = el.querySelector('[class*="titlesWrapper"], [data-name="legend-source-title"]');
+        const lines = norm(t ? (t.innerText || t.textContent) : '').split('\\n').map(norm).filter(Boolean);
+        const valueEls = Array.from(el.querySelectorAll('[class*="valueValue"]'));
+        const legacy = valueEls.length ? [] : Array.from(el.querySelectorAll('[class*="valueItem"]'));
         return {
           index: idx,
-          title: titleEl ? titleEl.textContent.trim() : '',
-          values: valueEls.map(v => v.textContent.trim()),
+          title: lines.length ? lines[0] : '',
+          args: lines.slice(1),
+          values: (valueEls.length ? valueEls : legacy).map(v => norm(v.innerText || v.textContent)),
         };
       });
     })()
