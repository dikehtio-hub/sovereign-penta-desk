# Phemex API credentials -- INTENTIONALLY EMPTY.
#
# This file previously contained a live key and secret in tracked source. It has
# zero importers (verified), so blanking it breaks nothing.
#
# Do not paste credentials here. This file is tracked, so anything written here
# enters git history permanently. Put credentials in an untracked .env instead
# and read them with os.getenv().
#
# OUTSTANDING (AGENTS.md item 532, ratified): the historical key/secret remain in
# git history at root commit 743496b. Blanking does not remove them. Rotate the
# Phemex credentials, then delete this file -- and only then is a git remote safe.

key = ""
secret = ""
